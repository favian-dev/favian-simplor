import { SplControlValidatorDirective } from './spl-control-validator.directive';

describe('SplControlValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SplControlValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
