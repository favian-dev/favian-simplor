import { SplTextareaDirective } from './spl-textarea.directive';

describe('RTextareaDirective', () => {
  it('should create an instance', () => {
    const directive = new SplTextareaDirective();
    expect(directive).toBeTruthy();
  });
});
