import { SplEffectDirective } from './spl-effect.directive';

describe('SplEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new SplEffectDirective();
    expect(directive).toBeTruthy();
  });
});
