import { SplContrastEffectDirective } from './spl-contrast-effect.directive';

describe('SplContrastEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new SplContrastEffectDirective();
    expect(directive).toBeTruthy();
  });
});
