import { SplTableDirective } from './spl-table.directive';

describe('SplTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SplTableDirective();
    expect(directive).toBeTruthy();
  });
});
