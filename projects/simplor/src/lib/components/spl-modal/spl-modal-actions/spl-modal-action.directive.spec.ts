import { SplModalActionDirective } from './spl-modal-action.directive';

describe('SplModalActionDirective', () => {
  it('should create an instance', () => {
    const directive = new SplModalActionDirective();
    expect(directive).toBeTruthy();
  });
});
