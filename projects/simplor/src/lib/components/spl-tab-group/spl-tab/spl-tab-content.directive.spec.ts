import { SplTabContentDirective } from './spl-tab-content.directive';

describe('SplTabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SplTabContentDirective();
    expect(directive).toBeTruthy();
  });
});
