import { SplTabLabelDirective } from './spl-tab-label.directive';

describe('SplTabLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new SplTabLabelDirective();
    expect(directive).toBeTruthy();
  });
});
