import { SplStrokeButtonDirective } from './spl-stroke-button.directive';

describe('SplStrokeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SplStrokeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
