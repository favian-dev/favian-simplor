import { SplFlatButtonDirective } from './spl-flat-button.directive';

describe('SplFlatButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SplFlatButtonDirective();
    expect(directive).toBeTruthy();
  });
});
