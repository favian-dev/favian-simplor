import { SplButtonDirective } from './spl-button.directive';

describe('SplButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SplButtonDirective();
    expect(directive).toBeTruthy();
  });
});
