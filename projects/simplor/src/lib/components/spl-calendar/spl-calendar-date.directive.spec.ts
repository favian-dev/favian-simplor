import { SplCalendarDateDirective } from './spl-calendar-date.directive';

describe('SplCalendarDateDirective', () => {
  it('should create an instance', () => {
    const directive = new SplCalendarDateDirective();
    expect(directive).toBeTruthy();
  });
});
