import { SplInputDirective } from './spl-input.directive';

describe('SplInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SplInputDirective();
    expect(directive).toBeTruthy();
  });
});
