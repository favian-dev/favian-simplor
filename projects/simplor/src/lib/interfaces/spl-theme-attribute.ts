import { SplTheme } from '../utils/type.util';

/** Interface for Directive or Component that accepts theme attributes. */
export interface SplThemeAttribute {
  /** It is a theme attribute that can be set. */
  theme: SplTheme;
}
